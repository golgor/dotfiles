# User-level CLI tools managed by ~/.dotfiles.
#
# This is linked into ~/.config/mise/conf.d so mise shims resolve these tools
# from any directory, not only inside ~/.dotfiles.

[tools]
atuin = "18.19.0"
kubectl = "1.36.2"
kubectx = "0.11.0"
kubens = "0.11.0"
fnox = "latest"
